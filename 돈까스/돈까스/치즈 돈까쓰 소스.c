#include<stdio.h>
int main(void) {
	/*int a, b;

	printf("두개의 정수를 입력해주세요:");
	scanf_s("%d %d", &a,&b);

	printf("몫:%d 나머지:%d", a / b, a % b);
	*/
	
	/*double a, b;

	printf("실수를 입력하세요:");
	scanf_s("%lf %lf", &a, &b);
	printf("%f %f %f %f", a + b, a - b, a * b, a / b);*/

	/*int a, b, c, max;
	printf("3개의 정수를 입력하세요:");
	scanf_s("%d %d %d",&a,&b,&c);
	max = a > b ? a : b;
	max = b > c ? b : c;

	printf("최대값:%d",max);
*/
	/*int a,i,p;	
	i = 2.54;
	printf("키를 입력하세요(cm):");
	scanf_s("%d", &a);
	//1피트는 12인치 
	//1인치는 2.54cm
	p = 24.54;
	printf("%dcm는 %d피트 %f인치 입니다.",a, );*/
	
	/*int a;
	printf("정수를 입력하세요:");
	scanf_s("%d", &a);
	printf("십의자리:%d\n일의자리:%d", a / 10, a % 10);*/
	
	/*ini;
	printf("정수를 입력하세요:");
	scanf_s("%d", &i);
	printf("2의 보수:%d",~i+1);*/
	
	/*int a,b;
	printf("정수를 입력하세요:");
	scanf_s("%d", &a);
	printf("2를 곱하고 싶은 횟수:");
	scanf_s("%d", &b);
	printf("%a << %d 의 값 : %d", a , b ,a<<b);
	*/

	//float r;
	//int p = 3.14;

	//printf("구의 반지름을 입력하세요:");
	//scanf_s("%f", &r);

	//printf("표면적은 %f입니다.", 4 * p * (r * r));
	//printf("체적은 %f입니다.", ( / 3) * p * r * r * r);

	

}